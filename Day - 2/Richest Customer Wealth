class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        richman = 0
        for temp in accounts:
            total = sum(temp)
            richman = max(richman,total)
            
        return richman
