class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        sums = 0
        for i in range(len(nums)):
            if i==0:
                nums[i] = nums[i]
            else:
                nums[i] = nums[i]+nums[i-1]
            
        
        return nums
